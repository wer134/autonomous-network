# 커밋된 체크포인트(agents/*.zip, *.pt)는 Python 3.14 / SB3 2.8.0 / torch 2.11 / numpy 2.4
# 환경에서 생성됐다 (ppo_network.zip 내부 system_info.txt). 이전 고정 버전
# (torch 2.3.1 / numpy 1.26.4 / sb3 2.3.2)으로는 numpy 2 피클(`numpy._core`)을 읽지 못해
# PPO 체크포인트 로드가 실패한다 (cowork/AUDIT_2026-09-09.md O7). 하한만 고정한다.
gymnasium>=0.29.1,<1.3
stable-baselines3>=2.8.0
torch>=2.6
numpy>=2.0,<3
pandas>=2.2.3
scikit-learn>=1.6
matplotlib>=3.9
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
